server:
    port: 8083

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/mobile_bg?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true
        username: root
        password: monbat
    #(create, create-drop, update)
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect